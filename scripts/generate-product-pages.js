// Script to generate product pages for all products (DISABLED - use dynamic routing instead)
console.log('Product page generation disabled - using dynamic routing instead');
console.log('Products will be served via /store/product/[slug] dynamic route');
process.exit(0);
